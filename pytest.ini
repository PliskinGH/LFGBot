[pytest]
# Treat plain `async def` test/`fixture` functions as asyncio tests automatically.
asyncio_mode = auto
# Make the project root importable (so `common`, `cogs` resolve from tests).
pythonpath = .
# Discover test files under the tests/ folder.
testpaths = tests